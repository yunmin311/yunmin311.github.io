# 个人网站 — 任务清单(tasks)

日期:2026-07-16。上游:`2026-07-16-implementation-plan.md`。
逐条执行,做完勾掉;每条都写清"做完的标志",不达标不算完。
执行中发现要偏离规格 → 先停下改文档、经用户点头(宪法第八条)。

## 阶段 1:骨架

- [x] **T1 项目改名**:`package.json` 的 `name` 改为 `personal-site`
  完成标志:`npm run build` 仍通过
- [x] **T2 站点与双语配置**:`astro.config.mjs` 填 `site`,配 i18n(中文默认、双前缀、根路径跳转)
  完成标志:访问 `/` 自动到 `/zh/`;`/zh/`、`/en/` 各自出页
- [x] **T3 界面文字字典**:`src/i18n/ui.ts`,导航/页脚/按钮等界面文字两语齐全,站名占位在此
  完成标志:两语言键一一对应,漏键会在构建时报错
- [x] **T4 视觉变量**:`src/styles/tokens.css` 建立(颜色、字号、间距,最朴素一套)
  完成标志:后续组件零散写死颜色字号 = 违规,评审时检查
- [x] **T5 全站布局**:`src/layouts/Base.astro` + 页头(站名、导航不常驻、EN/中 切换)+ 页脚
  完成标志:中英首页壳子互切正常;手机宽度导航一行排开不破版

## 阶段 2:内容集合

- [ ] **T6 集合定义**:`src/content.config.ts` 定义 `works`/`posts`,字段照规格第五节
  完成标志:故意写错一个字段,构建报错且报得明白
- [ ] **T7 占位内容**:作品 3 条(1 条带视频、1 条仅中文)、文章 2 条、草稿 1 条,全部标注"占位"
  完成标志:目录结构与规格第五节一致

## 阶段 3:页面

- [ ] **T8 首页**:网格,介绍块嵌为一格 + 精选作品(`featured` 按 `order` 排)+ 三个入口
  完成标志:中英两版各自只显示本语言的精选;介绍块与作品格平起平坐
- [ ] **T9 作品列表页**:同款网格,非草稿按日期倒序,封面统一比例(暂 4:3)+标题+简介
  完成标志:仅中文的作品不出现在英文列表;任意比例原图不破网格
- [ ] **T10 作品详情页**:头部字段(标题/年份/客户/角色/外链,选填不显示)+ 单列大图正文;
  有视频链接才渲染播放器;底部上/下一个作品翻页
  完成标志:带视频的占位作品能播;不带的无视频区域;翻页首尾不出错
- [ ] **T11 博客列表 + 文章页**:非草稿按日期倒序;文章页渲染 markdown
  完成标志:草稿文章在列表和直达网址都看不到
- [ ] **T12 关于/联系页**:照片位 + 介绍占位 + 状态行 + 邮箱防爬 + 社交链接位 + 中英简历 PDF 占位
  完成标志:网页源码里搜不到明文邮箱;页面上点邮箱能正常发信
  ⚠️ 真简历上线前必须提醒用户抹掉电话、住址等隐私信息(仓库公开、PDF 公开可下载)
- [ ] **T13 语言切换完整规则**:有翻译跳翻译,没翻译跳目标语言首页
  完成标志:在"仅中文作品"的详情页点切英文,落在英文首页而非 404
- [ ] **T14 404 页**:双语提示 + 回首页
  完成标志:乱敲网址能看到它
- [ ] **T15 SEO 基础**:每页标题/描述/og 卡片(作品页用封面、其余统一占位图)/hreflang 互指;
  sitemap(`@astrojs/sitemap`);favicon 换成站名首字母占位(去掉 Astro 火箭)
  完成标志:抽查页面源码四样俱全;sitemap 生成;标签页图标已非火箭
- [ ] **T16 阶段验收**:`npm run build` 通过;规格第八节 1–5 条逐条核对
  完成标志:给用户本地预览地址,用户过目

## 阶段 4:自动部署

- [ ] **T17 部署配置**:`.github/workflows/deploy.yml`(照计划技术备忘的版本号)
  完成标志:文件就位,`npm run build` 本地仍过
- [ ] **T18 开 Pages 开关**(用户操作,我给人话步骤):Settings → Pages → Source 选 "GitHub Actions"
  完成标志:用户确认点完
- [ ] **T19 上线验收**:推送 → GitHub 自动构建通过 → 打开正式网址
  完成标志:`https://yunmin311.github.io/` 公开可访问,规格第八节全过

## 记录

| 任务 | 状态 | 备注 |
|------|------|------|
| T1–T5 | ✅ 2026-07-16 | 阶段 1 完成:构建过、根路径跳 /zh/、EN·中互切、手机一行导航。当前语言标注改用加粗(下划线会让"中"变"虫") |
